/**
 * <p>Domain models representing plugins</p>
 *
 * <p>It's basically a merge of update center and statistics metadata. One big difference is the addition of categories
 * as they are an artificial property that is calculated at plugin generation time so it's possible to filter by
 * category without having to calcuate which labels to use.</p>
 */
package io.jenkins.plugins.models;
